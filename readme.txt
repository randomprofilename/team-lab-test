Исполняемый файл - app.js (либо через npm start)
БД - SQLite 3

При каждом перезапуске приложения таблицы в базе данных создаются заново (параметр force:true в sequelize.js).

Поскольку в задании не было указано каким образом хранить данные о счетах и картах, решил все сохранять в одном XML файле. 

С уважением, Ларионов Александр
